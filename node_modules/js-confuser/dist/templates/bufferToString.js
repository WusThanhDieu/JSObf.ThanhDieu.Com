"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.GetGlobalTemplate = exports.BufferToStringTemplate = void 0;
var _constants = require("../constants");
var _template = _interopRequireDefault(require("./template"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
const GetGlobalTemplate = exports.GetGlobalTemplate = (0, _template.default)(`
  function ${_constants.placeholderVariablePrefix}CFG__getGlobalThis${_constants.predictableFunctionTag}(){
    return globalThis
  }

  function ${_constants.placeholderVariablePrefix}CFG__getGlobal${_constants.predictableFunctionTag}(){
    return global
  }

  function ${_constants.placeholderVariablePrefix}CFG__getWindow${_constants.predictableFunctionTag}(){
    return window
  }

  function ${_constants.placeholderVariablePrefix}CFG__getThisFunction${_constants.predictableFunctionTag}(){
    return new Function("return this")()
  }

  function {getGlobalFnName}(array = [
    ${_constants.placeholderVariablePrefix}CFG__getGlobalThis${_constants.predictableFunctionTag},
    ${_constants.placeholderVariablePrefix}CFG__getGlobal${_constants.predictableFunctionTag},
    ${_constants.placeholderVariablePrefix}CFG__getWindow${_constants.predictableFunctionTag},
    ${_constants.placeholderVariablePrefix}CFG__getThisFunction${_constants.predictableFunctionTag}
  ]){
    var bestMatch
    var itemsToSearch = []
    try {
      bestMatch = Object
      itemsToSearch["push"](("")["__proto__"]["constructor"]["name"])
    } catch(e) {

    }
    A: for(var i = 0; i < array["length"]; i++) {
      try {
        bestMatch = array[i]()
        for(var j = 0; j < itemsToSearch["length"]; j++) {
          if(typeof bestMatch[itemsToSearch[j]] === "undefined") continue A;
        }
        return bestMatch
      } catch(e) {}
    }

		return bestMatch || this;
  }
`);
const BufferToStringTemplate = exports.BufferToStringTemplate = (0, _template.default)(`

  ${GetGlobalTemplate.source}

  var __globalObject = {getGlobalFnName}() || {};
  var __TextDecoder = __globalObject["TextDecoder"];
  var __Uint8Array = __globalObject["Uint8Array"];
  var __Buffer = __globalObject["Buffer"];
  var __String = __globalObject["String"] || String;
  var __Array = __globalObject["Array"] || Array;

  var utf8ArrayToStr = (function () {
    var charCache = new __Array(128);  // Preallocate the cache for the common single byte chars
    var charFromCodePt = __String["fromCodePoint"] || __String["fromCharCode"];
    var result = [];

    return function (array) {
        var codePt, byte1;
        var buffLen = array["length"];

        result["length"] = 0;

        for (var i = 0; i < buffLen;) {
            byte1 = array[i++];

            if (byte1 <= 0x7F) {
                codePt = byte1;
            } else if (byte1 <= 0xDF) {
                codePt = ((byte1 & 0x1F) << 6) | (array[i++] & 0x3F);
            } else if (byte1 <= 0xEF) {
                codePt = ((byte1 & 0x0F) << 12) | ((array[i++] & 0x3F) << 6) | (array[i++] & 0x3F);
            } else if (__String["fromCodePoint"]) {
                codePt = ((byte1 & 0x07) << 18) | ((array[i++] & 0x3F) << 12) | ((array[i++] & 0x3F) << 6) | (array[i++] & 0x3F);
            } else {
                codePt = 63;    // Cannot convert four byte code points, so use "?" instead
                i += 3;
            }

            result["push"](charCache[codePt] || (charCache[codePt] = charFromCodePt(codePt)));
        }

        return result["join"]('');
    };
  })();

  function {name}(buffer){
    if(typeof __TextDecoder !== "undefined" && __TextDecoder) {
      return new __TextDecoder()["decode"](new __Uint8Array(buffer));
    } else if(typeof __Buffer !== "undefined" && __Buffer) {
      return __Buffer["from"](buffer)["toString"]("utf-8");
    } else {          
      return utf8ArrayToStr(buffer);
    }
  }
`);